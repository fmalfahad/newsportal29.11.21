import 'package:flutter/material.dart';

class sceanshort extends StatelessWidget {
  const sceanshort({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // ignore: prefer_const_constructors
      body: Padding(
        padding: const EdgeInsets.all(8.0),
        child: Text('kaj baki roicha'),
      ),
    );
  }
}
